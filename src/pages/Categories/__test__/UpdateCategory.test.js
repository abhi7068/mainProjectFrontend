import UpdateCategory from '../UpdateCategoryPage';
import { render } from '../../../utils';
import React from 'react';
it('it renders', () => {
  render(<UpdateCategory match={{ params: '' }} />);
});
